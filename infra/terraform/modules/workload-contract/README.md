# Workload safety contract module

This provider-independent module freezes conservative development bounds before the individual Azure workload resources exist. It rejects environment or ownership overlap, always-on minimum replicas, broad database ingress, weak TLS, unbounded backup settings, anonymous Blob access, and unused static website hosting.

The values are safety ceilings and candidate settings, not approval of any Azure SKU, region, price, or deployment.
