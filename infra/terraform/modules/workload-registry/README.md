# Disposable workload registry

Creates one low-cost candidate ACR with administrative credentials, local users, anonymous pull, and task network bypass disabled. Container Apps authenticate through managed identity and explicit `AcrPull` role assignments. Basic is a planning candidate only and requires current support/price validation before any live plan.
