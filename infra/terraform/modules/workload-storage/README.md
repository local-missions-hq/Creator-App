# Disposable workload storage

Creates one narrowly firewalled Standard LRS account and exactly three private Blob containers for mission media, locality evidence, and exports. HTTPS/TLS 1.2, OAuth-first authorization, disabled Shared Key/local users/anonymous access, infrastructure encryption, versioning, and bounded soft delete are mandatory. Public networking is temporary and default-deny until the deferred private-network phase.
