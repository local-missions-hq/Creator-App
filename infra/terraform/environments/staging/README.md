# Staging environment

Private-network staging configuration will live here only after the ordinary infrastructure/UI path and the staging security gate are complete. No backend or resources are configured yet.
