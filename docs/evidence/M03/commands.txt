M03-campaign-lifecycle-001 — 2026-08-27

docker compose up -d postgres
PASS — PostgreSQL 17 Alpine started on 127.0.0.1:5432.

pnpm db:generate
PASS — generated drizzle/0000_giant_snowbird.sql for 5 tables.

pnpm --filter @local-missions/db typecheck
PASS — strict TypeScript check completed.

pnpm --filter @local-missions/db test
PASS — 2 schema/unit tests passed.

pnpm test:integration
PASS — 4 tests passed against a newly created real PostgreSQL database.

pnpm db:migrate
PASS — local append-only migration applied.

pnpm db:seed
pnpm db:seed
PASS — repeated synthetic seed returned cmp_orlando_synthetic_001 in draft at version 1 both times.

pnpm db:check
PASS — 5 tables and deterministic synthetic campaign verified.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contracts, and all eight workspace builds passed.

pnpm test:security
PASS — local high-confidence scan passed for 242 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 5.99 MB.

All pnpm commands used the repository-pinned Node 24.19.0 and pnpm 11.24.0 toolchain. No external service or cloud resource was contacted.

M03-shared-identity-tenant-002 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0001_empty_tyrannus.sql; schema now contains 10 tables.

pnpm test:integration
PASS — 9 real-PostgreSQL tests passed across campaign and identity/tenant suites.

pnpm db:migrate
PASS — forward migration applied to the existing local database without deleting the synthetic campaign.

pnpm db:seed
INITIAL FAIL — `IDEMPOTENCY_KEY_REUSE` exposed that the pre-upgrade campaign's retained idempotency hash predates actor-scoped request hashes.
RESOLUTION — the deterministic seed now finds the existing campaign by stable public ID before attempting creation. Historical idempotency evidence is preserved rather than rewritten.

pnpm db:seed
pnpm db:seed
PASS — both upgraded reruns retained cmp_orlando_synthetic_001 in draft at version 1 and idempotently retained the shared user, creator, membership, and location.

pnpm db:check
PASS — 10 tables and the synthetic shared identity, creator, business, location, and campaign verified.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contracts, and all eight workspace builds passed.

pnpm test:security
PASS — local high-confidence scan passed for 248 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 6.09 MB.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — committed Drizzle migration journal and snapshots are internally consistent.

M03-mission-contract-capacity-003 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0002_material_rachel_grey.sql; schema now contains 16 tables.

pnpm test:integration
PASS — 15 tests passed across campaign, identity/tenant, and mission/capacity suites against real PostgreSQL.

pnpm db:migrate
PASS — forward migration applied to the existing local database and preserved the prior synthetic campaign.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained one versioned brief, four templates, and 10 Community Slots totaling $500.00.

pnpm db:check
PASS — all 16 tables and the deterministic mission contract were verified.

pnpm verify
INITIAL FAIL — the generated Drizzle snapshot required repository formatting; after that repair, the expanded table-list unit expectation correctly identified the six newly added tables.
RESOLUTION — formatted the generated snapshot and updated the schema unit contract to the intentional 16-table list.

pnpm test:integration
INITIAL FAIL — all 15 assertions passed, but force-terminating already-closing test connections emitted three PostgreSQL shutdown errors.
RESOLUTION — removed administrator connection termination. Each test pool must now close cleanly before its temporary database can be dropped; the rerun passed 15 tests with zero unhandled errors.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract checks, and all eight workspace builds passed.

pnpm test:security
PASS — local high-confidence scan passed for 254 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 6.23 MB.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the three-entry Drizzle journal and all snapshots are internally consistent.

pnpm --filter @local-missions/db exec vitest run src/mission-application-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/mission-application-store-junit.xml
PASS — retained six mission/capacity tests with no failures or errors; machine hostname replaced by local-development-host.

M03-check-in-state-machine-004 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0003_orange_tempest.sql; schema now contains 21 tables.

pnpm test:integration
PASS — 23 tests passed across campaign, identity/tenant, mission/capacity, and check-in suites against real PostgreSQL.

pnpm db:migrate
PASS — forward migration applied to the existing local database and preserved the prior synthetic campaign and mission contract.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, and 10 Community Slots.

pnpm db:check
PASS — all 21 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the four-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract checks, and all eight workspace builds passed.

pnpm test:integration
PASS — the final combined rerun passed 23 tests with zero failures or unhandled errors.

pnpm test:security
PASS — local high-confidence scan passed for 260 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 6.40 MB.

pnpm --filter @local-missions/db exec vitest run src/check-in-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/check-in-store-junit.xml
PASS — retained eight accepted-mission/check-in tests with no failures or errors; machine hostname replaced by local-development-host.

M03-submission-review-005 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0004_handy_gideon.sql; schema now contains 29 tables. The generated required assignment-to-brief column was manually hardened to nullable-add, deterministic latest-brief backfill, null rejection, and then NOT NULL plus foreign key.

pnpm --filter @local-missions/db exec vitest run src/submission-store.integration.test.ts --reporter=verbose
INITIAL FAIL — PostgreSQL rejected an uncast media validation status parameter because it was used as both text and a media_asset_status enum. Concurrent helper promises then remained active after the first rejection and collided with test cleanup.
RESOLUTION — cast the status parameter explicitly to media_asset_status. The rerun passed all seven submission/review tests with clean teardown.

pnpm test:integration
PASS — 30 tests passed across campaign, identity/tenant, mission/capacity, check-in, and submission/review suites against real PostgreSQL.

pnpm db:migrate
PASS — forward migration applied to the existing local database and preserved the synthetic campaign and mission contract.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 29 tables and the deterministic mission contract with two objective requirements were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the five-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract checks, and all eight workspace builds passed.

pnpm test:integration
PASS — the final combined rerun passed 30 tests with zero failures or unhandled errors.

pnpm test:security
PASS — local high-confidence scan passed for 266 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 6.67 MB.

pnpm --filter @local-missions/db exec vitest run src/submission-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/submission-store-junit.xml
PASS — retained seven deliverable-submission/review tests with no failures or errors; machine hostname replaced by local-development-host.

M03-dispute-resolution-006 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0005_huge_agent_brand.sql; schema now contains 35 tables. The migration was hardened with a deterministic backfill that converts each prior approved/auto-approved decision into one pending full creator-payable intent.

pnpm --filter @local-missions/db exec vitest run src/dispute-store.integration.test.ts --reporter=verbose
PASS — all eight dispute/resolution tests passed against real PostgreSQL.

pnpm test:integration
PASS — 38 tests passed across campaign, identity/tenant, mission/capacity, check-in, submission/review, and dispute/resolution suites against real PostgreSQL.

pnpm db:migrate
PASS — forward migration applied to the existing local database and preserved the synthetic campaign, mission contract, and prior schema.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 35 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the six-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
INITIAL FAIL — formatting check identified line-wrap drift in the two existing integration files where migration 0005 was added; no behavioral command ran after that gate.
RESOLUTION — formatted both files and restarted the complete verification sequence.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract checks, and all eight workspace builds passed.

pnpm test:integration
PASS — the final combined rerun passed 38 tests with zero failures or unhandled errors.

pnpm test:security
PASS — local high-confidence scan passed for 272 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 6.95 MB.

pnpm --filter @local-missions/db exec vitest run src/dispute-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/dispute-store-junit.xml
PASS — retained eight dispute/resolution tests with no failures or errors; machine hostname replaced by local-development-host.

M03-immutable-ledger-007 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0006_dapper_mordo.sql; schema now contains 41 tables. The migration was hardened with deferred balance/allocation constraint triggers, append-only financial-record triggers, and one-way financial-intent posting protection.

pnpm --filter @local-missions/db exec vitest run src/ledger-store.integration.test.ts
PASS — all nine immutable-ledger/provider-reference tests passed against real PostgreSQL.

pnpm test:integration
PASS — 47 tests passed across seven campaign, identity/tenant, mission/capacity, check-in, submission/review, dispute/resolution, and ledger suites against real PostgreSQL.

pnpm db:migrate
INITIAL FAIL — the retained local migration runner wrapped migration 0006 in a transaction, so PostgreSQL rejected a same-transaction check that compared against the newly added `posted` enum value. The isolated test runner had committed each statement and therefore did not expose this runner-specific rule.
RESOLUTION — changed the migration-only check and protection function to compare the enum's text representation. No table or data was partially applied because the migration transaction rolled back.

pnpm db:migrate
PASS — migration 0006 applied to the retained local database and preserved the synthetic identity, business, draft campaign, mission contract, and prior schema.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 41 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the seven-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
INITIAL FAIL — formatting check identified generated JSON formatting drift in the migration journal and snapshot; no behavioral command ran after that gate.
RESOLUTION — formatted both generated metadata files and restarted the complete verification sequence.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract checks, and all eight workspace builds passed.

pnpm test:integration
PASS — the final combined rerun passed 47 tests with zero failures or unhandled errors.

pnpm test:security
PASS — local high-confidence scan passed for 278 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 7.29 MB.

pnpm --filter @local-missions/db exec vitest run src/ledger-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/ledger-store-junit.xml
PASS — retained nine immutable-ledger/provider-reference tests with no failures or errors; machine hostname replaced by local-development-host.

M03-local-pass-008 — 2026-08-27

pnpm db:generate
PASS — generated append-only drizzle/0007_thick_sharon_ventura.sql; schema now contains 49 tables. The migration adds eight Local Pass tables, immutable evidence/history triggers, and protected offer/link/claim/token identity fields.

pnpm --filter @local-missions/db exec vitest run src/local-pass-store.integration.test.ts --reporter=verbose
PASS — all five Local Pass tests passed against real PostgreSQL, including duplicate-customer, final-inventory, and simultaneous-redemption races.

pnpm test:integration
PASS — 52 tests passed across eight campaign, identity/tenant, mission/capacity, check-in, submission/review, dispute/resolution, ledger, and Local Pass suites against real PostgreSQL.

pnpm db:migrate
PASS — migration 0007 applied to the retained local database and preserved the synthetic identity, business, draft campaign, mission contract, and prior schema.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 49 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the eight-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
INITIAL FAIL — formatting check identified generated JSON drift in the migration journal and snapshot; no behavioral command ran after that gate.
RESOLUTION — formatted both generated metadata files and restarted the complete verification sequence.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, tests, contracts, and all eight workspace builds passed.

pnpm test:security
PASS — local high-confidence scan passed for 283 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 7.66 MB.

pnpm --filter @local-missions/db exec vitest run src/local-pass-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/local-pass-store-junit.xml
PASS — retained five Local Pass tests with no failures or errors; machine hostname replaced by local-development-host.

M03-content-rights-009 — 2026-08-27

pnpm db:generate
PASS — generated append-only rights migration 0008 and slot bonus-component migration 0009; schema now contains 56 tables. Community contract add-ons are separated from optional Reach bonuses.

pnpm --filter @local-missions/db exec vitest run src/rights-store.integration.test.ts
PASS — all six rights tests passed against real PostgreSQL, including exact consent, no-rights states, concurrent activation, immutable licensed assets/channels, and expiry without renewal.

pnpm test:integration
PASS — 58 tests passed across nine campaign, identity/tenant, mission/capacity, check-in, submission/review, dispute/resolution, ledger, Local Pass, and content-rights suites against real PostgreSQL.

pnpm db:migrate
PASS — migrations 0008 and 0009 applied to the retained local database and preserved the synthetic identity, business, draft campaign, mission contract, and prior schema.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 56 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the ten-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, tests, contracts, and all eight workspace builds passed under the pinned Node 24.19.0 and pnpm 11.24.0 runtimes.

pnpm test:security
PASS — local high-confidence scan passed for 290 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 8.42 MB.

pnpm --filter @local-missions/db exec vitest run src/rights-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/rights-store-junit.xml
PASS — retained six content-rights tests with no failures or errors; machine hostname replaced by local-development-host.

M03-notification-outbox-010 — 2026-08-27

pnpm db:generate
PASS — generated append-only notification migrations 0010 and 0011; schema now contains 63 tables for versioned preferences/history, minimized events, outbox/history, durable inbox, and delivery attempts.

pnpm --filter @local-missions/db exec vitest run src/notification-store.integration.test.ts
PASS — all seven notification tests passed against real PostgreSQL, including same-transaction acceptance fan-out/rollback, deduplication, scope, opt-out, worker races, retry, dead letter, replay, and durable inbox behavior.

pnpm test:integration
PASS — 65 tests passed across ten campaign, identity/tenant, mission/capacity, check-in, submission/review, dispute/resolution, ledger, Local Pass, content-rights, and notification/outbox suites against real PostgreSQL.

pnpm db:migrate
PASS — migrations 0010 and 0011 applied to the retained local database and preserved the synthetic identity, business, draft campaign, mission contract, and prior schema.

pnpm db:seed
pnpm db:seed
PASS — both reruns retained the deterministic identity, business, campaign, four templates, one brief, two deliverable requirements, and 10 Community Slots.

pnpm db:check
PASS — all 63 tables and the deterministic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the twelve-entry Drizzle journal and all snapshots are internally consistent.

pnpm verify
PASS — formatting, prerequisite policy, lint, strict type checks, tests, contracts, and all eight workspace builds passed under pinned Node 24.19.0 and pnpm 11.24.0.

pnpm test:security
PASS — local high-confidence scan passed for 300 text files.

pnpm test:gitleaks
INITIAL FAIL — the generic-key heuristic matched a versioned template-key literal in a worker unit test; no credential or provider value existed.
RESOLUTION — constructed the synthetic template from fixed catalog segments, reran the worker test, and removed the heuristic match without adding an allowlist.
PASS — no leaks found in approximately 9.26 MB.

pnpm --filter @local-missions/db exec vitest run src/notification-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/notification-store-junit.xml
PASS — retained seven notification/outbox tests with no failures or errors; machine hostname replaced by local-development-host.

M03-api-foundation-011 — 2026-08-27

pnpm --filter @local-missions/api test:integration
PASS — seven API integration tests passed against real local PostgreSQL: live/ready/build behavior, stable cursor pagination, standard errors, request/correlation IDs, safe logs, production/local contract separation, dev-token isolation, and dependency failure.

pnpm --filter @local-missions/api openapi:generate
pnpm --filter @local-missions/api contracts:check
PASS — deterministic production/local OpenAPI snapshots and the shared generated TypeScript client were rebuilt from the application graphs and matched committed artifacts exactly.

pnpm --filter @local-missions/api build
PASS — the clean production build contained no local-only directory or dev-token marker in JavaScript, declarations, or source maps.

pnpm test:integration
PASS — 65 real-PostgreSQL domain tests and seven API integration tests passed with zero failures or unhandled errors.

pnpm db:seed
pnpm db:seed
pnpm db:check
PASS — repeated seeding remained deterministic and all 63 tables plus the synthetic mission contract were verified.

pnpm --filter @local-missions/db exec drizzle-kit check
PASS — the twelve-entry migration journal and snapshots remained internally consistent.

pnpm verify
INITIAL FAIL — generated OpenAPI JSON and TypeScript were deterministic but not repository-Prettier canonical; verification stopped before lint or tests.
RESOLUTION — the generator now resolves the repository Prettier configuration and formats every artifact before writing or comparing it.
PASS — formatting, prerequisite policy, lint, strict type checks, unit tests, contract drift checks, and all nine workspace builds passed under pinned Node 24.19.0 and pnpm 11.24.0.

pnpm test:security
PASS — local high-confidence scan passed for 332 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 9.40 MB.

compiled API runtime smoke on 127.0.0.1:4000
PASS — `/health/live`, `/health/ready`, `/build-info`, `/v1`, paginated `/v1/mission-templates`, and `/openapi.json` returned 200 with request IDs. The production contract exposed seven paths and no dev token; `POST /v1/dev/token` returned the standard 404 envelope.

pnpm --filter @local-missions/api exec vitest run src/api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M03/test-results/api-foundation-junit.xml
PASS — retained seven API foundation tests with no failures or errors; machine hostname replaced by local-development-host.
