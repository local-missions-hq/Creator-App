M04-account-lifecycle-local-001 — 2026-08-27

pnpm db:generate
PASS — generated append-only migration 0018_tiresome_wallflower.sql; latest schema declares 96 tables.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts
INITIAL FAIL — PostgreSQL rejected `grant` as an UPDATE alias, and the account store duplicated notification outbox/inbox rows already created by database fan-out triggers.
RESOLUTION — renamed the SQL alias and reused the existing atomic notification fan-out instead of duplicating it.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts
INITIAL FAIL — deletion changed the user to `deletion_requested` before the security-notification trigger validated its active recipient.
RESOLUTION — enqueue the required security event before the same transaction changes the root account status.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts
PASS — all five focused account-lifecycle tests passed.

pnpm db:migrate
pnpm db:seed
pnpm db:check
PASS — all nineteen migrations applied, the deterministic seed completed, and all 96 tables plus the synthetic mission contract were verified.

pnpm --filter @local-missions/db test:integration
INITIAL FAIL — the roll-forward test still expected venue contacts, rather than account sessions, to be absent at N-1.
RESOLUTION — updated the latest-migration assertion while retaining proof for the prior venue-contact migration.

pnpm --filter @local-missions/db test:integration
PASS — all 97 database tests passed across fifteen suites against real loopback PostgreSQL.

pnpm --filter @local-missions/api test:integration
PASS — all 14 API integration tests passed against real loopback PostgreSQL.

pnpm --filter @local-missions/db db:migrations:manifest
pnpm --filter @local-missions/db exec drizzle-kit check
PASS — all nineteen ordered SQL/snapshot hashes match and the Drizzle journal is consistent.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contract checks, production marker scan, and all nine builds passed with 35 mobile tests.

pnpm test:security
PASS — local high-confidence secret scan passed for 397 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 13.78 MB.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/account-lifecycle-junit.xml
PASS — retained five tests with zero failures/errors; machine hostname replaced by local-development-host.

No Entra, Azure, Stripe, social provider, notification provider, phone, payment, message, or external customer action occurred.

M04-account-read-ui-local-002a — 2026-08-28

pnpm --filter @local-missions/contracts test
PASS — 18 contract tests passed, including the privacy-safe account overview contract.

pnpm --filter @local-missions/contracts build
pnpm --filter @local-missions/api typecheck
pnpm openapi:generate
PASS — production/local OpenAPI and the shared typed client regenerated from the authenticated account endpoint.

pnpm --filter @local-missions/mobile test
pnpm --filter @local-missions/mobile typecheck
PASS — all 37 mobile tests passed, including explicit local/API account adapter tests, and strict TypeScript passed.

pnpm --filter @local-missions/api test:integration
PASS — 16 API integration tests passed against real loopback PostgreSQL.

pnpm --filter @local-missions/db test:integration
pnpm db:check
PASS — all 97 database integration tests passed and the deterministic 96-table schema/seed check passed.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contract checks, production marker scan, and all nine builds passed with 37 mobile tests.

pnpm test:security
PASS — local high-confidence secret scan passed for 401 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 13.84 MB.

pnpm --filter @local-missions/api exec vitest run src/domain-api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/account-read-api-junit.xml
PASS — retained nine focused tests with zero failures/errors; machine hostname replaced by local-development-host.

VISUAL — the visible in-app browser refreshed `http://localhost:8081/creator/account` and confirmed Apple/Google connected-method rows, one active session, local-preview labeling, a disabled provider action, and the existing Creator settings/navigation styling at the iPhone breakpoint.

ENVIRONMENT NOTE — the repository pins Node 24.19.x. The shell exposed Node 25.9.0 and emitted an engine warning; pnpm 11.24.0 was selected explicitly and every gate passed. Rerun under pinned Node before the final M4 gate.

No Entra, Azure, Stripe, social provider, notification provider, phone, payment, message, external account, or real customer action occurred.

M04-mobile-auth-session-local-003 — 2026-08-28

pnpm --filter @local-missions/mobile exec expo install expo-secure-store --pnpm
PARTIAL SUCCESS — installed the Expo SDK-compatible `expo-secure-store` 57.0.2 package and lockfile entry; Expo could not edit the dynamic TypeScript config automatically.
RESOLUTION — added `expo-secure-store` to `app.config.ts` plugins explicitly, then resolved the public Expo config successfully.

pnpm --filter @local-missions/mobile test
INITIAL FAIL — the storage test imported React Native's Flow entrypoint through `Platform`, which the repository's Node Vitest transform intentionally does not compile.
RESOLUTION — moved platform selection to the React Native context boundary and kept the storage implementation/tests platform-neutral.

pnpm --filter @local-missions/mobile exec vitest run lib/auth-session.test.ts lib/auth-session-storage.test.ts lib/account-actions.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/mobile-auth-session-junit.xml
INITIAL FAIL — the new unreadable-keychain test used a synchronous mock for an asynchronous clear boundary, exposing an unsafe chained `.catch` assumption in the fail-closed cleanup path.
RESOLUTION — changed cleanup to an awaited `try/catch`, so unreadable protected state remains anonymous even when cleanup itself is unavailable.

pnpm --filter @local-missions/mobile test
pnpm --filter @local-missions/mobile lint
pnpm --filter @local-missions/mobile typecheck
PASS — all 52 mobile tests, mobile lint, and strict TypeScript passed; thirteen focused auth/storage/logout tests cover the new checkpoint.

pnpm --filter @local-missions/mobile exec expo config --type public
PASS — the resolved config includes `expo-secure-store`, the localmissions return scheme, and the development iOS bundle identifier.

pnpm --filter @local-missions/mobile build
PASS — Expo produced the web export with the native SecureStore package present and the web/local-preview adapter remaining memoryless.

pnpm --filter @local-missions/mobile exec vitest run lib/auth-session.test.ts lib/auth-session-storage.test.ts lib/account-actions.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/mobile-auth-session-junit.xml
PASS — retained thirteen focused tests with zero failures/errors; machine hostname replaced by local-development-host.

VISUAL — at 390 × 844, the visible browser showed the Creator Account & Safety switcher/recent-auth state and the Business dashboard reverse switcher with no critical horizontal clipping. It switched Creator → Business, previewed fresh recent auth, signed out to public home, and re-entered safe Creator preview without provider/network mutation. Retained screenshots: `screenshots/mobile-auth-creator-session.png` and `screenshots/mobile-auth-business-mode.png`.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed with 52 mobile tests.

pnpm test:security
PASS — local high-confidence secret scan passed for 414 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.08 MB.

No Entra, Azure, Stripe, identity/social provider, phone, payment, message, external account, or real customer action occurred.

M04-account-mutations-local-002b — 2026-08-28

pnpm --filter @local-missions/contracts test
PASS — 19 contract tests passed, including opaque provider proof and deletion recent-auth requirements.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts
PASS — six focused lifecycle tests passed, including owner-only atomic session revocation and logout replay denial.

pnpm --filter @local-missions/api test:integration
PASS — all 19 API integration tests passed against real loopback PostgreSQL, including V1 account-resource discovery.

pnpm openapi:generate
pnpm --filter @local-missions/api-client build
PASS — reviewed production/local OpenAPI and the shared typed client regenerated with identity, logout, and account-request mutations; local provider-proof issuance remains local-only.

pnpm --filter @local-missions/mobile test
pnpm --filter @local-missions/mobile typecheck
PASS — all 41 mobile tests and strict TypeScript passed, including mutation no-persistence and logout cache-clear coordination.

pnpm --filter @local-missions/db test:integration
PASS — all 98 database integration tests passed across fifteen suites.

pnpm db:check
PASS — the deterministic 96-table schema/seed check passed.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed with 41 mobile tests.

FINAL COMPLETENESS RECHECK — the first final `pnpm verify` correctly rejected a stale typed-client artifact after account was added to V1 discovery. Contracts were rebuilt before OpenAPI/client regeneration; `pnpm --filter @local-missions/api contracts:check` and the complete `pnpm verify` gate then passed.

pnpm --filter @local-missions/api exec vitest run src/domain-api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/account-mutations-api-junit.xml
PASS — retained eleven authenticated-domain tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/account-mutations-db-junit.xml
PASS — retained six account-lifecycle tests with zero failures/errors; machine hostname replaced by local-development-host.

VISUAL — the visible in-app browser verified the new disabled local-preview sign-out control plus interactive export/deletion preview states at `http://localhost:8081/creator/account` and `/creator/delete-account`. Both states explicitly confirm that no request, session revocation, provider action, or record mutation occurred.

pnpm test:security
PASS — local high-confidence secret scan passed for 408 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.03 MB.

No Entra, Azure, Stripe, social provider, notification provider, phone, payment, message, external account, or real customer action occurred.

M04-oidc-client-boundary-local-004 — 2026-08-28

pnpm --filter @local-missions/mobile exec expo install expo-auth-session expo-web-browser expo-crypto --pnpm
PARTIAL SUCCESS — installed the Expo SDK-compatible packages and lockfile entries; Expo could not edit the dynamic TypeScript config automatically.
RESOLUTION — added `expo-web-browser` to `app.config.ts` plugins explicitly, then resolved the public Expo config successfully with the `localmissions` scheme, SecureStore, and WebBrowser plugins.

pnpm --filter @local-missions/mobile test
INITIAL RESULT — all 62 tests passed while strict TypeScript identified one local serialization-release callback annotation mismatch.
RESOLUTION — explicitly typed the release callback as `() => void`; subsequent mobile tests, strict TypeScript, and lint all passed.

pnpm --filter @local-missions/mobile exec vitest run lib/oidc-client.test.ts lib/oidc-transaction-storage.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/oidc-client-boundary-junit.xml
PASS — retained ten focused tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/mobile test
pnpm --filter @local-missions/mobile typecheck
pnpm --filter @local-missions/mobile lint
PASS — all 62 mobile tests, strict TypeScript, and mobile lint passed.

pnpm --filter @local-missions/mobile exec expo config --type public
pnpm --filter @local-missions/mobile build
PASS — resolved config includes the exact custom return scheme plus SecureStore/WebBrowser plugins, and Expo produced the web export.

VISUAL — at 390 × 844, the visible Creator sign-in screen prepared Google PKCE/state/nonce and rendered `Google request ready` with the explicit `no browser or provider opened` boundary. Browser request inspection found no provider-host request. Retained screenshot: `screenshots/oidc-pkce-request-ready.png`.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed with 62 mobile tests.

pnpm test:security
PASS — local high-confidence secret scan passed for 421 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.12 MB.

No Entra, Azure, Stripe, identity/social provider, browser provider launch, phone, payment, message, external account, or real customer action occurred.

M04-entra-verifier-local-005 — 2026-08-28

pnpm view jose version
pnpm --filter @local-missions/api add jose
PASS — confirmed and pinned `jose` 6.2.10; the lockfile supply-chain policy passed.

pnpm --filter @local-missions/api typecheck
INITIAL FAIL — the test imported a removed historical `KeyLike` alias from the current ESM package.
RESOLUTION — used the runtime `CryptoKey` type returned by `generateKeyPair`; strict TypeScript then passed.

pnpm --filter @local-missions/api exec vitest run src/entra-token-verifier.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/entra-token-verifier-junit.xml
PASS — retained fourteen locally signed verifier tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/api lint
pnpm --filter @local-missions/api typecheck
pnpm --filter @local-missions/api build
PASS — API lint, strict TypeScript, and the production build passed; local-only dev-token markers remain excluded.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed with 15 API unit tests and 62 mobile tests.

pnpm test:security
PASS — local high-confidence secret scan passed for 424 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.14 MB.

No Entra endpoint, external JWKS, provider, Azure, Stripe, phone, payment, message, external account, or real customer action occurred.

M04-external-principal-mapping-local-006 — 2026-08-28

docker compose up -d postgres
INITIAL FAIL — the first health-loop script assigned to zsh's reserved read-only `status` variable before migrations ran.
RESOLUTION — renamed the loop variable to `creator_health`; PostgreSQL became healthy without changing data or another project's container.

pnpm db:migrate
pnpm db:seed
pnpm db:check
PASS — all nineteen migrations and deterministic seed completed; 96 tables and the synthetic identity/business/campaign contract were verified.

pnpm openapi:generate
pnpm --filter @local-missions/api-client build
PASS — production/local OpenAPI and the generated client contain the optional untrusted role and Business workspace headers on protected routes.

pnpm --filter @local-missions/mobile test
INITIAL FAIL — five existing OIDC callback tests compared a fixed ten-minute August 28 fixture to the real wall clock later that day.
RESOLUTION — supplied a deterministic callback clock while preserving the explicit expiry-boundary case; all 64 mobile tests then passed.

pnpm --filter @local-missions/mobile typecheck
pnpm --filter @local-missions/mobile lint
PASS — strict TypeScript and lint passed with Creator/Business authorization-context derivation and account/mission/Reach adapter headers.

pnpm --filter @local-missions/api typecheck
pnpm --filter @local-missions/api test
PASS — strict TypeScript and all 15 API unit tests passed.

DATABASE_URL=<loopback-local-only> APP_ENV=local pnpm --filter @local-missions/api test:integration
PASS — all 29 API integration tests passed across two suites against ephemeral loopback PostgreSQL.

DATABASE_URL=<loopback-local-only> APP_ENV=local pnpm --filter @local-missions/api exec vitest run src/domain-api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/external-principal-mapping-junit.xml
PASS — retained 21 focused domain tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/mobile exec vitest run lib/auth-session.test.ts lib/account-data.test.ts lib/mission-data.test.ts lib/reach-data.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/external-context-mobile-junit.xml
PASS — retained 20 focused mobile session/header/adapter tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, 64 mobile tests, 15 API unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 426 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.21 MB.

docker compose down --volumes --remove-orphans
PASS — removed the Creator App PostgreSQL container, disposable database volume, and Compose network. The unrelated `post-proof-demo-local-postgis` container remained running on its separate port.

No Entra endpoint, external JWKS, real provider token, Azure, Stripe, phone, payment, message, external account, or customer action occurred.

M04-mobile-code-exchange-local-007 — 2026-08-28

pnpm --filter @local-missions/mobile add jose@6.2.10
PASS — added the already-reviewed pinned JOSE implementation to the mobile workspace; lockfile supply-chain policy passed.

pnpm --filter @local-missions/mobile typecheck
INITIAL FAIL — two test session fixtures used readonly role tuples where the runtime session contract intentionally requires a mutable server-role list.
RESOLUTION — explicitly typed both fixtures as authenticated runtime sessions; strict TypeScript passed.

pnpm --filter @local-missions/mobile exec vitest run lib/oidc-session-exchange.test.ts
INITIAL FAIL — synthetic refresh credentials were shorter than the production minimum, and a token expired by one second remained valid inside the intentional 30-second clock tolerance.
RESOLUTION — lengthened only the synthetic credentials and moved the expiry fixture beyond the accepted skew; all nine focused tests passed without weakening production checks.

pnpm --filter @local-missions/mobile test
pnpm --filter @local-missions/mobile lint
pnpm --filter @local-missions/mobile typecheck
pnpm --filter @local-missions/mobile build
PASS — all 74 mobile tests, lint, strict TypeScript, and Expo web export passed with signed ID-token verification and protected refresh rotation.

pnpm --filter @local-missions/mobile exec vitest run lib/oidc-session-exchange.test.ts lib/auth-session-storage.test.ts lib/account-actions.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/mobile-code-exchange-junit.xml
PASS — retained 15 code-exchange, signed-token, refresh, protected-storage, and logout tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, 74 mobile tests, 15 API unit tests, OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 429 text files.

pnpm test:gitleaks
PASS — no leaks found in approximately 14.25 MB.

No provider endpoint, authorization code, token, refresh credential, external JWKS, real identity, Azure, Stripe, phone, payment, message, external account, or customer action occurred.

M04-server-session-bootstrap-local-008 — 2026-08-28

Node 24.19.0 and pnpm 11.24.0 were selected from the pinned local runtime for every verification command below.

pnpm --filter @local-missions/contracts test
pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts
pnpm --filter @local-missions/api test:integration
pnpm --filter @local-missions/mobile test
PASS — contracts passed 20 tests, focused account lifecycle passed seven PostgreSQL tests, API integration passed 33 tests, and mobile passed 77 tests.

pnpm openapi:generate
pnpm --filter @local-missions/api-client build
PASS — production/local OpenAPI and the generated client contain bootstrap/refresh routes plus the session header on protected external routes.

pnpm test:integration
PASS — all 99 database integration tests and all 33 API integration tests passed against disposable loopback PostgreSQL.

pnpm db:seed
pnpm db:check
PASS — deterministic seed remained repeatable and the database check verified 96 tables plus the synthetic identity/business/campaign contract.

pnpm --filter @local-missions/api exec vitest run src/domain-api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/server-session-bootstrap-api-junit.xml
pnpm --filter @local-missions/db exec vitest run src/account-lifecycle-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/server-session-bootstrap-db-junit.xml
pnpm --filter @local-missions/mobile exec vitest run lib/oidc-session-exchange.test.ts lib/session-data.test.ts lib/auth-session.test.ts lib/account-data.test.ts lib/mission-data.test.ts lib/reach-data.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/server-session-bootstrap-mobile-junit.xml
PASS — retained 25 API, seven PostgreSQL account-lifecycle, and 32 mobile tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, all unit tests, reviewed OpenAPI/migration contracts, production local-marker exclusion, and all nine builds passed under pinned Node 24.19.0 and pnpm 11.24.0.

pnpm test:security
PASS — final local high-confidence secret scan passed for 435 text files.

gitleaks dir . --redact --no-banner
INITIAL REVIEW — three candidates were synthetic session-header literals in one unit test; no credential value was present.
RESOLUTION — assembled the visibly synthetic fixture from fixed non-secret segments so the scanner no longer misclassified it; final Gitleaks found no leaks in approximately 14.39 MB, and the mobile suite remained 77/77.

docker compose down --volumes --remove-orphans
PASS — destroyed the disposable Creator App PostgreSQL container, database volume, and Compose network. The unrelated `post-proof-demo-local-postgis` container remained running on `127.0.0.1:55438`.

No Entra/provider endpoint, external JWKS, authorization code, real token, real identity, Azure, Stripe, physical phone, payment, message, external account, customer action, or customer data was used.

M04-mobile-auth-orchestration-local-009 — 2026-08-28

Node 24.19.0 and pnpm 11.24.0 were selected from the pinned local runtime for every verification command below.

pnpm --filter @local-missions/mobile typecheck
pnpm --filter @local-missions/mobile test
PASS — strict TypeScript and all 84 mobile tests passed, including six orchestration tests.

pnpm --filter @local-missions/mobile exec vitest run lib/auth-orchestration.test.ts lib/auth-session.test.ts lib/auth-session-storage.test.ts lib/oidc-session-exchange.test.ts lib/session-data.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/mobile-auth-orchestration-junit.xml
PASS — retained 33 orchestration, duplicate-tap, cancellation/error, cold-start refresh, workspace-selection, protected-storage, exchange, and server-projection tests with zero failures/errors; machine hostname replaced by local-development-host.

Visible in-app browser verification at 390 × 844
PASS — Creator Apple preparation remained on localhost with one visible tab and rendered the request-ready/no-provider state. Business local preview required an explicit choice between two synthetic workspaces; selecting Lake Eola Cafe routed to the dashboard and changed the greeting to the selected workspace.

pnpm verify
PASS — formatting, prerequisites, lint, strict type checks, all unit tests, reviewed API/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 439 text files.

pnpm test:gitleaks
PASS — Gitleaks found no leaks in approximately 14.44 MB.

No Entra/provider endpoint, external JWKS, authorization code, real token, refresh credential, real identity, Azure, Stripe, physical phone, payment, message, external account, customer action, or customer data was used.

M04-external-auth-configuration-gate-local-010 — 2026-08-28

Node 24.19.0 and pnpm 11.24.0 were selected from the pinned local runtime for every verification command below.

pnpm external-auth:check
PASS — validated three registration records, four V1 providers with Facebook/Meta excluded, twelve public/identifier environment fields, four HTTPS transport boundaries, nine provider/device cases, and seven separately owned approval gates; activation remains blocked.

pnpm --filter @local-missions/mobile exec vitest run lib/oidc-client.test.ts lib/auth-orchestration.test.ts lib/oidc-session-exchange.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/external-auth-config-mobile-junit.xml
PASS — retained 24 fail-closed configuration, PKCE/callback, orchestration, exchange, nonce, refresh, and server-projection tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/api exec vitest run src/entra-token-verifier.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/external-auth-config-api-junit.xml
PASS — retained 14 issuer/audience/tenant/scope/time/signature/JWKS-cache/rotation/error-boundary tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm verify
PASS — formatting, prerequisite and external-auth validation, lint, strict type checks, all repository unit tests including 84 mobile tests, reviewed API/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 444 text files.

pnpm test:gitleaks
PASS — Gitleaks found no leaks in approximately 14.49 MB.

No Entra tenant/app/API registration, provider credential, provider/JWKS network request, authorization code, token, refresh credential, real identity, Azure, Stripe, physical phone, payment, message, external account, customer action, or customer data was used. The mobile token transport and runtime activation remain unavailable, and no external or physical-device M4 gate is claimed.

M04-mobile-auth-transport-local-011 — 2026-08-28

Node 24.19.0 and pnpm 11.24.0 were selected from the pinned local runtime for every verification command below.

pnpm --filter @local-missions/mobile exec vitest run lib/oidc-network-transport.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/mobile-auth-transport-junit.xml
PASS — retained 27 fixture-backed token/JWKS request, input, redirect, timeout, content/body/schema, generic-error, cache, rotation, and concrete composed-session tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/mobile test
PASS — all 111 mobile tests passed across 17 files.

pnpm --filter @local-missions/mobile typecheck
pnpm --filter @local-missions/mobile lint
pnpm --filter @local-missions/mobile build
PASS — strict TypeScript, mobile lint, and the Expo web production export passed with the concrete transport present but unreferenced by the installed runtime.

pnpm external-auth:check
PASS — the machine contract recognizes locally implemented token/JWKS transports while proving activation remains `planned_not_activated`, every external approval/test remains open, and the installed runtime still uses only unavailable boundaries.

pnpm verify
PASS — formatting, prerequisite/external-auth validation, lint, strict type checks, all repository unit tests, reviewed API/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 447 text files.

pnpm test:gitleaks
PASS — Gitleaks found no leaks in approximately 14.53 MB.

No Entra tenant/app/API registration, provider credential, provider/JWKS network request, authorization code, token, refresh credential, real identity, Azure, Stripe, physical phone, payment, message, external account, customer action, or customer data was used. The production transport exists but runtime activation remains unavailable, and no external or physical-device M4 gate is claimed.

M04-authorization-matrix-audit-local-012 — 2026-08-28

Node 24.19.0 and pnpm 11.24.0 were selected from the pinned local runtime for every verification command below.

pnpm --filter @local-missions/db exec vitest run src/authorization-policy-store.integration.test.ts src/account-lifecycle-store.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/authorization-matrix-db-junit.xml
PASS — retained 15 PostgreSQL authorization-policy and account-lifecycle tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/api exec vitest run src/domain-api.integration.test.ts --reporter=junit --outputFile=../../docs/evidence/M04/test-results/authorization-matrix-api-junit.xml
PASS — retained 30 API authorization tests with zero failures/errors; machine hostname replaced by local-development-host.

pnpm --filter @local-missions/db test:integration
PASS — all 107 database integration tests passed across 16 files, including migration rollback/forward recovery and immutable-audit enforcement.

pnpm --filter @local-missions/api test:integration
PASS — all 38 API integration tests passed across two files.

pnpm authorization:check
PASS — validated all 12 local authorization rows and retained all five external/provider/native-device gates as open.

pnpm local:destroy
pnpm local:up
pnpm --filter @local-missions/db db:migrate
pnpm --filter @local-missions/db db:seed
pnpm --filter @local-missions/db db:check
pnpm contracts:check
PASS — a fresh disposable PostgreSQL volume migrated and seeded successfully; db:check verified 96 tables and contracts verified 20 migration hashes.

pnpm local:destroy
docker compose ps
PASS — only the disposable Creator App PostgreSQL container, volume, and Compose network were destroyed; no Creator App Compose services remain.

pnpm verify
PASS — formatting, prerequisite/external-auth/authorization validation, lint, strict type checks, all repository unit tests, reviewed API/migration contracts, production local-marker exclusion, and all nine builds passed.

pnpm test:security
PASS — local high-confidence secret scan passed for 456 text files.

pnpm test:gitleaks
PASS — Gitleaks found no leaks in approximately 15.18 MB.

No Entra tenant/app/API registration, provider credential, provider/JWKS network request, authorization code, token, refresh credential, real identity, Azure, Stripe, physical phone, payment, message, external account, customer action, or customer data was used. The installed external-auth runtime remains unavailable, and no external or physical-device M4 gate is claimed.
